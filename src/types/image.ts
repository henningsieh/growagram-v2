export enum PhotosSortField {
  UPLOAD_DATE = "createdAt",
  CAPTURE_DATE = "captureDate",
}

export enum PhotosSortOrder {
  ASC = "asc",
  DESC = "desc",
}

export enum PhotosViewMode {
  PAGINATION = "pagination",
  INFINITE_SCROLL = "infinite-scroll",
}
